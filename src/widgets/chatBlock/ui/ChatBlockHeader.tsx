import styles from "../ChatBlock.module.css";

export const ChatBlockHeader = () => {
    return (
        <header className={styles.header}>
            <h2 className={styles.title}>Чарты</h2>
        </header>
    )
}
