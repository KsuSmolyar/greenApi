import styles from "../ChartBlock.module.css";

export const ChartBlockHeader = () => {
    return (
        <header className={styles.header}>
            <h2 className={styles.title}>Чарты</h2>
        </header>
    )
}
